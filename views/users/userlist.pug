extends ../head 

block content
    main
        .cuadro.ancho-largo 
            .header-card
                h1 Listado de usuarios
                
            section.body
                div
                    a.btn-action(href="/user/create"): i.glyphicon.glyphicon-plus
                table.table
                    thead
                        tr
                            th No.
                            th Usuario
                            th Rol
                            th.text-center Herramientas 
                    tbody
                        each usuario, i in listausuario
                            tr
                                td= i+1
                                td= usuario.user
                                td= usuario.rol
                                td.text-center  
                                    a.btn-action(href="/user/edit/" + usuario.id): i.glyphicon.glyphicon-pencil
                                    a.btn-action(onclick="confirmaruser("+usuario.id+")"): i.glyphicon.glyphicon-trash
                        else
                            tr
                                td
                                    p No existe ningun ususario
block scriptes
   script(src='/js/script.js')
          
                